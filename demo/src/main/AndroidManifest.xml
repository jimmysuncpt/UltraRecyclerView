<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.jimmysun.ultrarecyclerview"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".demo.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".demo.HorizontalActivity"
            android:label="Horizontal Demo"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".demo.VerticalActivity"
            android:label="Vertical Demo"
            android:screenOrientation="portrait"/>
    </application>

</manifest>